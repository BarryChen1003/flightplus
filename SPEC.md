# FlightPlus 設計規格書（Design Spec）

> 基於 Open Design 流程預備。本規格作為實作時的對接文件。

---

## 1. 品牌與色彩系統

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-primary` | `#1a56db` | 主按鈕、連結、強調 |
| `--color-primary-dark` | `#1342b0` | Hover 狀態 |
| `--color-primary-light` | `#e8effe` | 背景点缀、分隔 |
| `--color-surface` | `#ffffff` | 卡片、面板背景 |
| `--color-bg` | `#f8fafc` | 頁面背景 |
| `--color-text` | `#0f172a` | 主文字 |
| `--color-text-muted` | `#64748b` | 次要文字、說明 |
| `--color-border` | `#e2e8f0` | 邊框、分隔線 |
| `--color-success` | `#10b981` | 成功狀態 |
| `--color-warning` | `#f59e0b` | 警告、優惠標籤 |
| `--color-error` | `#ef4444` | 錯誤狀態 |

**字體**：`Inter`（UI）、`Noto Sans TC`（中文）\
**圓角**：`--radius-sm: 6px`、`--radius-md: 12px`、`--radius-lg: 20px`\
**陰影**：`--shadow-sm`、`--shadow-md`（卡片陰影）

---

## 2. 頁面結構

### 2.1 首頁（Landing Page）

```
┌─────────────────────────────────────────────────────┐
│ NAV: Logo | [機票 | 飯店] 切換 | 幣別 | 登入/註冊  │
├─────────────────────────────────────────────────────┤
│                                                     │
│   HERO（全螢幕背景圖）                               │
│   「找遍全球最優惠的機票和飯店」                      │
│   「即時比價，涵蓋 300+ 航空公司與飯店平台」          │
│                                                     │
│   ┌─────────────────────────────────────────────┐   │
│   │  起點 ▾ │ 目的地 ▾ │ 出發 ─ 回程 │ 人數 │  │   │
│   │              [ 搜尋機票 ]                    │   │
│   └─────────────────────────────────────────────┘   │
│                                                     │
├─────────────────────────────────────────────────────┤
│  三欄特色                                         │
│  [⚡ 快速比價] [💰 聯盟回饋] [🧠 智能推薦]          │
├─────────────────────────────────────────────────────┤
│  航班搜尋結果（點擊搜尋後進入）                      │
├─────────────────────────────────────────────────────┤
│  合作夥伴 LOGO 牆                                    │
├─────────────────────────────────────────────────────┤
│  FOOTER：聯盟計畫 | 聯絡資訊 | 隱私權               │
└─────────────────────────────────────────────────────┘
```

### 2.2 航班搜尋結果頁（/search）

```
┌──────────────────────────────────────────────────────────┐
│ NAV（同首頁）                                            │
├────────────┬─────────────────────────────────────────────┤
│  左側篩選器  │  右側結果列表                              │
│  (280px)   │  ┌──────────────────────────────────────┐  │
│            │  │ EVA BR 551  │ TPE→NRT │ 08:30→14:05  │  │
│ 價格範圍    │  │                            NT$4,200 │  │
│ 航空公司    │  │ 直飛  2h35m        [查看詳情]        │  │
│ 轉機次數    │  └──────────────────────────────────────┘  │
│ 航班時段    │  ┌──────────────────────────────────────┐  │
│            │  │ 中華 CI 134 │ TPE→NRT │ 10:00→16:40  │  │
│ ─────────  │  │                            NT$3,850   │  │
│  [清除]     │  │ 1次轉機  6h40m     [查看詳情]        │  │
│  [套用]     │  └──────────────────────────────────────┘  │
└────────────┴─────────────────────────────────────────────┘
```

### 2.3 飯店搜尋結果頁（/hotels）

```
┌────────────┬─────────────────────────────────────────────┐
│  左側篩選器  │  飯店卡片網格（2欄 → 1欄手機）              │
│            │  ┌────────────┐ ┌────────────┐              │
│  星級       │  │ 飯店圖片   │ │ 飯店圖片   │              │
│  評分       │  │ ⭐⭐⭐⭐ 台北喜來登 │ ⭐⭐⭐⭐⭐ 遠東飯店  │   │
│  價格/晚    │  │ 8.7 (1,234則)│ │ 9.1 (876則) │              │
│  設施       │  │ NT$3,200/晚  │ │ NT$5,600/晚  │              │
│  （WiFi/   │  │ [查看詳情]   │ │ [查看詳情]   │              │
│   停車/     │  └────────────┘ └────────────┘              │
│   早餐）    │                                              │
└────────────┴─────────────────────────────────────────────┘
```

---

## 3. 核心元件規格

### 3.1 NavBar
- 高度：`64px`，`position: sticky top: 0`
- 背景：`rgba(255,255,255,0.95)` + `backdrop-filter: blur(8px)`
- Logo 左側，搜尋類型切換（機票/飯店）在中央偏左
- 右側：貨幣選擇（USD/TWD/HKD/JPY）| 登入/註冊按鈕

### 3.2 Hero Section
- 高度：`100vh - 64px`（去掉 NavBar）
- 背景：飛機/旅行相關圖片，`linear-gradient` 疊加讓文字可讀
- 標題：`H1`，`clamp(2rem, 5vw, 3.5rem)`，白色
- 搜尋表單：白色卡片，`border-radius: 20px`，`box-shadow: var(--shadow-lg)`

### 3.3 FlightCard（航班卡片）
- `border-radius: 12px`，白色背景，`border: 1px solid var(--color-border)`
- 左側：航空公司 Logo（40×40）+ 名稱
- 中間：起飛/抵達時間 + 機場 + 飛行時間
- 右側：價格（大字 `#1a56db`）+ 「查看詳情」按鈕
- 直飛標籤：綠色背景；轉機標籤：橙色背景

### 3.4 HotelCard（飯店卡片）
- `border-radius: 12px`，`overflow: hidden`
- 圖片高度：`200px`，`object-fit: cover`
- 星級用 `★` 顯示，評分用數字 + 灰底白字徽章
- 價格：右下角，`NT$3,200` 格式

### 3.5 搜尋表單
- 機票版：`起點[機場] | 目的地[機場] | 出發[日期] | 回程[日期] | 人數[下拉] | [搜尋]`
- 飯店版：`目的地[城市] | 入住[日期] | 退房[日期] | 人數/房數 | [搜尋]`
- 手機版：點擊「搜尋」按鈕 → 全螢幕彈出表單（modal）

---

## 4. 響應式斷點

| 斷點 | 行為 |
|------|------|
| `< 640px` | 單欄，側邊篩選折疊為彈出式，搜尋表單全螢幕 modal |
| `640px - 1024px` | 兩欄，篩選器折疊到頂部橫條 |
| `> 1024px` | 三欄佈局（左側篩選固定，右側結果列表） |

---

## 5. 動畫與微互動

- **Hover**：卡片 `transform: translateY(-2px)` + 陰影加深，150ms ease
- **按鈕**：`scale(0.97)` 按下效果
- **頁面載入**：機票卡片 staggered fade-in，`animation-delay: 100ms` 遞進
- **篩選更新**：結果區域 `opacity: 0.5 → 1` 過渡，表示正在刷新

---

## 6. 技術實作方向

- **Framework**：Next.js 15 App Router（`app/` 目錄結構）
- **Styling**：Tailwind CSS，客製化 `tailwind.config.ts` 加入品牌色彩
- **圖標**：`lucide-react`（輕量、一致的 UI 圖標庫）
- **機場選擇**：未來串接 Travelpayouts City API，近期用 `daisyui` Autocomplete 或 `react-select`
- **狀態管理**：Next.js Server Components + React `useState`/`useReducer`
- **圖片**：`next/image` 優化，飯店圖來自 Travelpayouts CDN 或 Unsplash 備用

---

## 7. 頁面路由對照

| 路由 | 頁面 | 狀態 |
|------|------|------|
| `/` | 首頁（Hero + 搜尋） | ✅ 實作中 |
| `/search` | 航班搜尋結果 | ⬜ 待實作 |
| `/hotels` | 飯店搜尋結果 | ⬜ 待實作 |
| `/flight/:id` | 航班詳情（含聯盟連結） | ⬜ 待實作 |
| `/hotel/:id` | 飯店詳情 | ⬜ 待實作 |

---

## 8. 實作優先順序

1. **P0**（MVP 上線必備）：首頁 Hero + 搜尋表單 + 航班結果頁
2. **P1**（變現核心）：飯店結果頁 + 聯盟連結追蹤
3. **P2**（差異化）：7 策略引擎結果鑲嵌